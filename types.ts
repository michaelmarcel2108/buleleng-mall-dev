export interface Business {
  id: string;
  name: string;
  desc: string;
}

export interface Product {
  id: string;
  name: string;
  shopee_id: string;
  shopee_url: string;
}
export interface category {
  id: string;
  name: string;
}
